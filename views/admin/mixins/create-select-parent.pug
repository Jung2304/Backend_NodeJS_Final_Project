mixin findParent(arr, prefix="")
  if(arr)
    each item in arr 
      option(value=item.id)= `${prefix}${item.title}`
      if(item.children && item.children.length > 0)
        +findParent(item.children, prefix + "-- ")
